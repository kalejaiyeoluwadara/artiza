/**
 * Every console screen reads through `useAdminList` and draws its own
 * skeletons, so a second, differently-shaped placeholder here would be a flash
 * of the wrong layout before the right one.
 */
export default function Loading() {
  return null;
}
